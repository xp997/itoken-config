spring:
  application:
    name: itoken-config
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/xp997/itoken-config-repository
          search-paths: respo
          username:
          password:
  zipkin:
      base-url: http://localhost:8780

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/